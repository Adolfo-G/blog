{{#each posts as |post| }}
      <div class="card" style="width: 85%;margin-left: auto;margin-right: auto;">
        <h2>{{post.title}}</h2>
        <div class="card-body">
          <p class="card-text">{{post.content}}</p>
        </div>
      </div>
{{/each}}
